<div class="row">
  
  <div class="small-4 columns">
    <!-- MAIN CONTENT GOES HERE -->
    <%= render :partial => "location_details" %>
  </div>

  <div class="small-4 columns">
    <!-- SIDEBAR CONTENT GOES HERE -->
    <%= render partial: 'camps_using' %>
  </div>

  <div class="small-4 columns">
    <%= render partial: 'dynamic_map', locals: {lat: @location.latitude, lon: @location.longitude, name: @location.name} %>
  </div>

</div>